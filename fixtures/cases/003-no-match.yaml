id: no-match
description: No rules match - URL unchanged
url: "http://example.com/other"
rules: |
  RewriteEngine On
  RewriteRule ^foo$ /bar [R=301,L]
serverVariables:
  SERVER_NAME: "example.com"
  HTTP_HOST: "example.com"
expected:
  outputUrl: "http://example.com/other"
  statusCode: null
  trace:
    - line: "RewriteEngine On"
      reached: true
      met: true
      valid: true
    - line: "RewriteRule ^foo$ /bar [R=301,L]"
      reached: true
      met: false
      valid: true
