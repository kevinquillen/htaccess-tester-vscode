id: redirect-302
description: 302 temporary redirect
url: "http://example.com/temp"
rules: |
  RewriteEngine On
  RewriteRule ^temp$ /permanent [R=302,L]
serverVariables:
  SERVER_NAME: "example.com"
  HTTP_HOST: "example.com"
expected:
  outputUrl: "http://example.com/permanent"
  statusCode: 302
  trace:
    - line: "RewriteEngine On"
      reached: true
      met: true
      valid: true
    - line: "RewriteRule ^temp$ /permanent [R=302,L]"
      reached: true
      met: true
      valid: true
